type SideBarProps = {};

const SideBar: React.FC<SideBarProps> = ({}) => {
  //   useEffect(() => {}, []);

  return (
    <>
      <div className="side h-[100vh] w-[10rem] sm:w-[15rem] bg-blue-400 left-0"></div>
    </>
  );
};

export default SideBar;
